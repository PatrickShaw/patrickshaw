#!/usr/bin/env bash
alias stop-all-containers="$(dirname $0)/docker/stop-all-containers.sh"
alias remove-all-containers="$(dirname $0)/docker/remove-all-containers.sh"
alias remove-all-images="$(dirname $0)/docker/remove-all-images.sh"
	